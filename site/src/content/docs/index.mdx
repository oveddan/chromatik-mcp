---
title: Chromatik MCP
description: AI-accelerated light-show composition and performance. Chat with an agent that composes live inside Chromatik — and sees what it makes.
template: splash
hero:
  tagline: >-
    AI-accelerated composition and performance for
    <a href="https://chromatik.co/">Chromatik</a>, the engine behind
    large-scale LED art. Drop one jar into a live show and chat — an agent
    builds the patterns, palettes, motion and macro rigs, while you keep the
    faders, the snapshots, and the undo stack.
  actions:
    - text: Get started
      link: /getting-started/
      icon: right-arrow
    - text: GitHub
      link: https://github.com/oveddan/chromatik-mcp
      icon: github
      variant: minimal
---

import { Card, CardGrid } from '@astrojs/starlight/components';

{/* TODO: embed demo video here once recorded — Claude Code driving a live Chromatik show. */}

<div class="chat-demo not-content">
  <div class="chat-line human">
    <span class="chat-who">you</span>
    <p>Give me a slow amber sunrise across the whole structure, with sparkles
    that ride the beat. Keep it subtle until the drop.</p>
  </div>
  <div class="chat-line agent">
    <span class="chat-who">agent</span>
    <p>Built a gradient channel with a 90-second amber→rose sweep, an LFO
    breathing the brightness, and a sparkle layer gated by the beat pulse.
    Its intensity is on <code>macro1</code> — pull it up when the drop hits.
    I rendered a frame to check: reading warm, but dim on the north face.
    Raising it 12%.</p>
  </div>
  <div class="chat-caption">
    Changes land live, on the engine thread — and unwind with Cmd-Z.
  </div>
</div>

## Compose by conversation

Any MCP client — Claude Code, Claude Desktop, Cursor, your own orchestrator —
connects straight into the running engine. No project-file editing, no reload
cycle, no separate server: the agent reads and mutates the same live show your
console renders. Describe a look and it builds the structure; ask for motion
and it wires the modulation; ask for control and it hands you a macro rig with
OSC addresses attached.

```sh
curl -L --create-dirs -o ~/Chromatik/Packages/chromatik-mcp.jar \
  https://github.com/oveddan/chromatik-mcp/releases/download/latest/chromatik-mcp.jar
# enable Chromatik-MCP in Preferences → Plugins, restart, then:
claude mcp add --transport http chromatik "http://127.0.0.1:$(jq -r .port ~/.chromatik-mcp/status.json)/mcp"
```

<CardGrid stagger>
  <Card title="It sees what it makes" icon="seti:image">
    `get_frame` renders the actual output — a compact color summary or a PNG
    the model literally looks at. The agent iterates against what the show
    looks like, not what it hopes it looks like.
  </Card>
  <Card title="It knows the instruments" icon="open-book">
    A generated semantic catalog describes what every pattern, effect, and
    modulator actually does — color modes, parameter interactions — with
    bytecode-hash staleness flags so it never bluffs about changed code.
  </Card>
  <Card title="Built for performance" icon="rocket">
    Tempo and beat-pulse wiring, macro knob banks, snapshots for whole looks,
    cue/aux preview buses, spatial views over the model's geometry. The agent
    composes the rig; you play it.
  </Card>
  <Card title="The human keeps the console" icon="approve-check">
    Mutations route through Chromatik's own undo system — an agent's session
    unwinds step by step with Cmd-Z. Concurrent agents are safe, and the show
    file is untouched until you save.
  </Card>
</CardGrid>

## Part of a bigger pattern

What [TouchDesigner MCP](https://github.com/8beeeaaat/touchdesigner-mcp) does
for TouchDesigner, chromatik-mcp does for Chromatik: put the agent interface
*inside* the creative tool's runtime, so AI becomes a collaborator in the
medium itself. Agentic workflows for artists — not just programmers.
