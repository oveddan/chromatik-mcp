# Semantic catalog format

The catalog gives MCP-connected agents *semantic* understanding of patterns, effects, and
modulators — what a class does visually/algorithmically and how its parameters interact.
Structural facts (parameter names, types, ranges, options, descriptions) are **never**
recorded here: the live tools (`get_parameter`, `list_available_*`) own structure and are
always current.

**Catalog entries are a cache of source understanding, keyed by content hashes.** They are
generated by the [`chromatik-mcp-catalog` skill](../.claude/skills/chromatik-mcp-catalog/SKILL.md), not
hand-maintained — hand-maintained docs drift silently; hashed generated docs go *detectably*
stale instead. The one exception is a declared [curated section](#curated-sections), which
trades that guarantee away deliberately and says so in the entry.

## Location

One markdown file per class, named by fully-qualified class name:

```
package/src/main/resources/catalog/<fqcn>.md
e.g. package/src/main/resources/catalog/heronarts.lx.pattern.color.GradientPattern.md
```

`resources/` makes each entry a plain Maven resource — it ships inside the plugin jar and
the runtime serves it from the classpath. **Only stock LX entries live here**: baking
content-repo docs into the chromatik-mcp jar would couple its releases to every content package.

*Temporary exception:* `apotheneum.*` entries (37 files) are bundled here too, as a stopgap
until [Apotheneum#38](https://github.com/oveddan/Apotheneum/pull/38) ships its own catalog
inside the Apotheneum jar (tier 2, below) and is released — at which point the bundled copies
must be deleted. This is a deliberate, tracked violation of the rule above, not a precedent.
Likewise the stock `heronarts.lx.*` entries here are dropped once
[heronarts/LX#153](https://github.com/heronarts/LX/pull/153) ships in the pinned LX release
and those entries move into the LX jar itself.

At runtime, resolving a class's entry has two stages: an outright-wins overlay check, then
collecting and ranking every classpath copy (tiers 2 and 3 below are no longer tried in
sequence — they're gathered together and ranked by accuracy, not by which one answers
first):

1. **`~/.chromatik-mcp/catalog/<fqcn>.md`** — the machine-local overlay (source:
   `"overlay"`). Where content-repo entries land when generated from local source (e.g.
   Apotheneum), and how any entry is refreshed without rebuilding a jar. Always wins
   outright if present — it is an explicit, deliberate override, not a candidate to be
   ranked against anything else.
2. **The class's own loader** (source: `"class-jar"`) — `clazz.getClassLoader()`. A
   content package can ship its own catalog inside its jar (generated by the same skill,
   packaged under `catalog/` in its resources): docs travel with the code they describe,
   and a drop-in jar arrives documented with zero chromatik-mcp involvement. This works
   whenever the class's own loader is the shared `LXClassLoader` that Chromatik loads all
   package jars into — true for content-package classes (e.g. `apotheneum.*`), since that
   loader's URL list includes every package jar, not just the one that defined the class.
3. **The registry's plugin-jar loader** (source: `"plugin-jar"`) — `lx.registry.getClassLoader()`,
   re-fetched on every lookup rather than captured once, so this loader keeps resolving
   after a Chromatik content reload disposes and replaces it. This loader exists because
   tier 2 is *not* symmetric: `heronarts.lx.*` classes are defined by the classloader that
   loaded the LX application itself — the **parent** of the shared `LXClassLoader` — and a
   `ClassLoader` can see resources on its own and its ancestors' classpaths, never a
   descendant's. So a stock LX class's own loader can never see `catalog/*.md` bundled
   inside a child classloader's jar, no matter how tier 2 is queried. Tier 3 routes the
   lookup through the shared `LXClassLoader` instead, which reaches every package jar on
   its URL list — **not just this one**: if two different package jars each ship a
   `catalog/<fqcn>.md` for the same stock class (e.g. once
   [heronarts/LX#153](https://github.com/heronarts/LX/pull/153) ships upstream and both
   LX's jar and this one carry an entry for the same class), tier 3 is exactly where both
   copies surface as separate candidates for ranking — it is not a loader that only ever
   sees chromatik-mcp's own bundled entries.
4. **Absent** — no entry found at any tier.

Tiers 2 and 3 each ask for **every** resource named `catalog/<fqcn>.md` they can see
(`ClassLoader.getResources`, plural — not the first hit), and the two loaders' results are
merged and deduplicated by URL before ranking (they overlap whenever a class's own loader
*is* the shared `LXClassLoader`, and delegation can otherwise surface the same physical
file from both). A candidate that fails to parse is logged and skipped; it cannot deny
service for a valid candidate found elsewhere.

**Ranking, most significant first:**

1. **Bytecode match.** The runtime hashes the live class's loaded bytecode (see
   [Staleness semantics](#staleness-semantics-served-by-get_component_doc) below) and
   prefers a candidate whose `classBytesSha256` equals it — the entry actually generated
   against the code now running.
2. **Newest `generatedAt`** (missing treated as oldest).
3. **A deterministic tiebreak on the candidate's URL string**, so the outcome never
   depends on filesystem or classloader enumeration order.

Proximity — which jar happened to define the class, or which jar a classloader's unordered
file listing surfaced first — is deliberately **not** authority here: nothing about *where*
an entry lives says anything about whether it was generated against the code actually
running. Only the ranking above decides that, which is why resolution always reports every
candidate it considered (`get_component_doc`'s `catalog.candidates`, never omitted, not
just the winner): **winner first, then the rest in ranked order.** When an overlay is
present it is the winner by outright override, not by ranking (see tier 1 above), so it can
sit ahead of a classpath candidate that would otherwise rank higher on bytecode match or
recency — "winner first" and "ranked first" agree except in that one case.

A class with no entry at any tier is simply *undocumented* (`documented: false` over
MCP) — never fabricate an entry for a class whose source is unavailable.

## Frontmatter

Flat `key: value` scalars only (the runtime parser is deliberately not a YAML
implementation — no nesting, no multi-line values; `tags` is comma-separated):

```markdown
---
class: heronarts.lx.pattern.color.GradientPattern
kind: pattern                      # pattern | effect | modulator
sourceRepo: LX                     # logical name; sources.json maps it to the GitHub URL
sourcePath: src/main/java/heronarts/lx/pattern/color/GradientPattern.java
sourceSha256: <64 hex>             # source file at generation time — the generator's incremental key
classBytesSha256: <64 hex>         # .class bytes at generation time — the runtime staleness key (optional)
classBytesOrigin: ~/.m2/repository/com/heronarts/lx/1.2.1/lx-1.2.1.jar
lxVersion: 1.2.1
generatedAt: 2026-07-09T00:00:00Z
generator: chromatik-mcp-catalog/2 (<model>)
curated: parameterInteractions, usageTips   # sections hand-written and preserved across runs (optional)
curatedAt: 2026-07-24T00:00:00Z             # required when `curated` is present
tags: color, gradient, palette
---
```

The `generator` major version tracks the *body format* below (`/2` = claim-bullet
format); bump it when the body contract changes so mixed-vintage catalogs are
distinguishable.

### Curated sections

`curated` names body sections written by hand because the insight is not reachable from
source — it came from driving the pattern on real hardware. Its value is a comma-separated
list drawn from exactly two tokens, matching the payload keys `get_component_doc` returns:
`parameterInteractions` and `usageTips`. The Summary is never curated — it must stay
derivable from source. `curatedAt` is required alongside it.

Curated prose still obeys every content rule below, including the ban on parameter names:
the point is to capture a *relationship* the source doesn't state plainly, not to restate
structure the live tools already serve.

**What this costs, stated plainly.** Two guarantees weaken, and both are accepted knowingly:

- *Preservation is an instruction to the generator, not a mechanism.* `CatalogFormatTest`
  validates that the keys are well-formed; nothing can force a regeneration run to actually
  carry the prose through. The skill's step 5 says to, and a run that ignores it silently
  loses the curation. Treat `curated` as a marker that makes the loss reviewable in a diff,
  not as write protection.
- *The staleness hash no longer covers the whole body.* A regeneration rewrites the
  uncurated sections and stamps a fresh `classBytesSha256`, so an entry can report
  `stale: false` over curated prose that was never re-derived from the new bytes. That is
  the precise drift this format exists to prevent, so `get_component_doc` surfaces
  `curated` in its `catalog` block: a client seeing it knows which sections the hash does
  not vouch for.

## Body: three fixed sections

```markdown
## Summary
One sentence: what this renders / does. Then atomic claim bullets.
- claim
- claim

## Parameter interactions
- how the knobs interact and which combinations matter — NOT a parameter list

## Usage tips
- when to reach for it, what it pairs well with, gotchas
```

The runtime splits on these exact headings. Content rules for generators:

- **One topic sentence, then claim bullets.** Each section opens with at most one
  plain sentence, followed by `- ` bullets where each bullet states exactly one
  verifiable claim. Atomic claims are individually checkable by the review pass and
  deny the generator room for filler narration. (Summary's topic sentence is the
  one-line answer to "what does this render?"; its bullets carry the rest.)
- **The claim filter**: every bullet must either help an agent *choose* this class or
  prevent a concrete *misuse* of it. Implementation narration that doesn't change what
  an operator does is cut — "how the code computes it" is out; the visible consequence
  is in.
- **Length budget**: Summary ≤ ~100 words; whole body ≤ ~250 words. Simple classes
  should come in far under budget — a three-bullet entry is a valid entry.
- **Live vs latched**: for each behavior-shaping control, when discernible from source,
  say whether it acts *continuously* (responds to live modulation/sweeping) or is
  *sampled* at trigger/spawn time. This one distinction is most of what a
  modulation-routing agent needs.
- **Optional mechanism sketch**: an algorithmically dense class may include one fenced
  code block immediately after the Summary bullets — short pseudocode of the core loop
  using *role names* ("density", "the spawn interval"), never source identifiers. Use
  it to replace prose narration, not to add to it.
- Describe **behavior**, not structure. Never restate parameter names/ranges/defaults —
  they can drift, and the live tools already answer that. The line: describing *what a
  mode does* ("wrap modes clip or reflect the chase at strip ends") is in scope — bare
  enumerations of names/values/ranges are not.
- **Every mechanism claim must be verified against the source** — the order of transform
  stages, which buffer is written, what a parameter scales. A confidently wrong mechanism
  is worse than no entry. Never reference a parameter that does not exist in the source.
- **Triage rule (modulators especially)**: if a class's live per-parameter descriptions
  are the whole story — pure value-holders, trivial comparators — skip the entry or
  write a minimal one. Entries earn their place with architecture, interactions, or
  selection knowledge the live tools cannot express.
- Written for an LLM audience: precise verbs, no marketing language.
- `tags`: behavior vocabulary (motion, color, audio-reactive, geometric, texture,
  masking, strobe, utility, …), lowercase, comma-separated.

## Staleness semantics (served by `get_component_doc`)

The runtime hashes the **loaded class bytecode** (via the class's own classloader) and
compares it to `classBytesSha256`:

- `stale: false` — bytes identical to generation time. Trustworthy: identical bytecode
  means identical behavior.
- `stale: true` — bytes differ. The doc *may* still be accurate (recompilation without a
  source change can flip this); erring toward stale is deliberate. Regenerate.
- `stale: "unknown"` — the entry has no recorded hash, or the class bytes aren't readable.

`sourceSha256` is not consulted at runtime — it is the generation-side key that lets the
skill skip classes whose source hasn't changed.
